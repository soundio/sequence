# Sequence tests

## Run tests

Run a test file:

```cli
deno run --allow-read --allow-import --reload test/sequence-iterator.js
```

Run all tests:

```cli
deno run --allow-read --allow-import --reload test/test.js
```
